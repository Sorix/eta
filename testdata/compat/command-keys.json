{
  "notes": [
    "Generated from eta command-key resolution in the repository root.",
    "Expected values are environment-specific where they include cwd or which(1) output; portableTemplate describes the rule."
  ],
  "cases": [
    {
      "name": "bare executable found on PATH",
      "command": "ls -la /tmp",
      "portableTemplate": "{cwd}\\n{which:ls} -la /tmp"
    },
    {
      "name": "absolute path executable",
      "command": "/usr/bin/env FOO=1",
      "portableTemplate": "/usr/bin/env FOO=1"
    },
    {
      "name": "unresolvable bare command",
      "command": "no_such_command_xyz --flag",
      "portableTemplate": "{cwd}\\nno_such_command_xyz --flag"
    },
    {
      "name": "multiple spaces normalize to one separator after executable",
      "command": "go  build",
      "portableTemplate": "{cwd}\\n{which:go} build"
    }
  ]
}
