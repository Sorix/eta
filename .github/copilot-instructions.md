# GitHub Copilot Instructions

When working in this repository, read and follow the canonical project instructions in `AGENTS.md` at the repository root.

`AGENTS.md` is the source of truth for build commands, project structure, CLI flags, design decisions, and repository conventions.
